
 function showPopup2(textVal) {
     alert(textVal);
 }
 
 function showWarning2(warnText) {
     // do something with warning text
     showPopup2(warntext);
 }
 
 function showError2(errorText) {
     // do something with error text
     showPopup2(errorText);
 }